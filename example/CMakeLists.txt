cmake_minimum_required(VERSION 3.5)
project(example)
include(../CMakeAda.cmake)

add_ada_library(
        ada_lib         # Plain name of the library being built (e.g. libada_lib.a) as generated by gprbuild
        adalibtest.gpr  # gpr file that builds it
        lib)            # Folder in which it is put by gprbuild (e.g. ./lib)

add_executable(c_main_using_ada_lib main.c)
target_link_libraries(c_main_using_ada_lib ada_lib gnat)

add_ada_executable(
        bin_test        # Name of the executable as generated by gprbuild
        adabintest.gpr) # gpr file that builds it
